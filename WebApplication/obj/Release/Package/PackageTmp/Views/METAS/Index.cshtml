@model IEnumerable<WebApplication.Models.METAS>
@using GridMvc.Html;

    @Html.Grid(Model).AutoGenerateColumns().Columns(Columns =>
{
Columns.Add().Encoded(false).Sanitized(false).SetWidth(30).RenderValueAs(d =>d.CONTRATO.NOME_CLIENTE);
Columns.Add().Encoded(false).Sanitized(false).SetWidth(30).RenderValueAs(d => @<b>@Html.ActionLink("Detalhes", "Details", new { id = d.ID_META })</b>);
}).Filterable(true)
</table>
